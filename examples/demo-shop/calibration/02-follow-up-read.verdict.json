{
  "id": "02-follow-up-read",
  "packet": "02-follow-up-read.packet.json",
  "shape": "a follow-up read whose body assertion observes the state the route serves; the seed reaches path",
  "rule": "an assertion on a response field only produced on that path, a follow-up read that observes the effect, or an assertion on a push effect",
  "expect": {
    "seeds": {
      "b52bb585": "path"
    },
    "upgrades": 1,
    "confirmed": 0,
    "rejections": {}
  },
  "verdict": {
    "route": "GET orders/v1/cart",
    "reader": "reference",
    "packetHash": "1fb4d4c81530",
    "seeds": [
      {
        "id": "U1",
        "key": "b52bb585",
        "level": "path",
        "evidence": [
          {
            "spec": "tests/checkout.spec.ts",
            "test": "lists the cart",
            "line": 19,
            "reason": "the test reads the cart back and asserts a field of its body, which only this route's read produces"
          }
        ]
      }
    ]
  }
}
